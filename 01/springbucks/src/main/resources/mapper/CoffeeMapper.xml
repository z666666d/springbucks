<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="geektime.spring.springbucks.waiter.mapper.CoffeeMapper">
    <resultMap id="BaseResultMap" type="geektime.spring.springbucks.waiter.model.Coffee">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Feb 08 00:02:18 CST 2019.
        -->
        <id column="ID" jdbcType="BIGINT" property="id" />
        <result column="NAME" jdbcType="VARCHAR" property="name" />
        <result column="PRICE" jdbcType="BIGINT" property="price" typeHandler="geektime.spring.springbucks.waiter.handler.MoneyTypeHandler" />
        <result column="CREATE_TIME" jdbcType="TIMESTAMP" property="createTime" />
        <result column="UPDATE_TIME" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>

    <insert id="save" parameterType="geektime.spring.springbucks.waiter.model.Coffee">
        insert into t_coffee (NAME,PRICE,CREATE_TIME,UPDATE_TIME)
        values (#{name}, #{price}, now(), now())
    </insert>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select ID,NAME,PRICE,CREATE_TIME,UPDATE_TIME FROM t_coffee where ID = #{id}
    </select>

    <select id="selectByIds" parameterType="java.util.List" resultMap="BaseResultMap">
        select ID,NAME,PRICE,CREATE_TIME,UPDATE_TIME FROM t_coffee
        <where>
            <if test="ids != null and ids.size() > 0">
                id in
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select ID,NAME,PRICE,CREATE_TIME,UPDATE_TIME FROM t_coffee order by ID
    </select>

    <select id="deleteById" parameterType="java.lang.Long">
        delete from t_coffee where ID = #{id}
    </select>

    <update id="updateById" parameterType="geektime.spring.springbucks.waiter.model.Coffee">
        update t_coffee set NAME = #{name},PRICE = #{price},UPDATE_TIME = now() where ID = #{id}
    </update>
</mapper>